@inherits LayoutComponentBase

<div class="page">
    <header class="topbar">
        <a class="brand" href="/">Gatekeeper</a>
        <AuthorizeView>
            <Authorized>
                <NavLink class="nav" href="/" Match="NavLinkMatch.All">Дашборд</NavLink>
                <NavLink class="nav" href="/queue">Очередь</NavLink>
                <NavLink class="nav" href="/history">История</NavLink>
                <NavLink class="nav" href="/questions">Вопросы</NavLink>
                <AuthorizeView Context="ownerContext" Roles="Owner">
                    <Authorized>
                        <NavLink class="nav" href="/visitors">Посетители</NavLink>
                    </Authorized>
                </AuthorizeView>
                <span class="user">@context.User.Identity?.Name</span>
                <form method="post" action="auth/logout" class="inline">
                    <AntiforgeryToken />
                    <button type="submit" class="link">Выйти</button>
                </form>
            </Authorized>
        </AuthorizeView>
    </header>
    <main>
        @Body
    </main>
</div>
